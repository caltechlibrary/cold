<!DOCTYPE html>
<html lang="en-US">
{{>head}}
<body>
{{>header}}
<nav>
<ul>
<li><a href="/" title="return home">Home</a></li>
<li><a href="../" title="return to dashboard">Dashboard</a></li>
<li><a href="./" title="return to funder list">Funders</a></li>
<li><a href="{{funder.clfid}}?view=edit">Edit {{funder.clfid}}</a></li>
</ul>
</nav>
<section>

<h1>{{funder.name}}</h1>
<div class="identifier">{{clfid}}</div>

{{#if funder.grant_numbers}}
<ul>
{{#each funder.grant_numbers}}
<li>({{this}})</li>
{{/each}}
</ul>
{{/if}}

{{#if funder.description}}<div class="description">{{funder.description}}</div><p>{{/if}}

{{#if funder.type}}<div class="type">organization Type: {{funder.type}}</div>{{/if}}

{{#if funder.url}}<div class="url">website: <a href="{{funder.url}}" title="go to funder website" target="_blank">{{funder.url}}</a></div>{{/if}}

{{#if funder.include_in_feeds}}<div class="feeds_link">View in feeds: <a href="https://feeds.library.caltech.edu/funders/{{funder.clfid}}" title="funder in feeds" target="_blank">{{funder.clfid}}</a></div>{{/if}}

{{#if funder.grant_numbers}}
<div id="grant_numbers" class="grant_numbers">{{#funder.grant_numbers}}
{{.}}
{{/funder.grant_numbers}}</div>
{{/if}}

<div class="identifiers">
  {{#if funder.ror}}<div class="ror">ror: <a href="{{funder.ror}}" title="go to ror.org" target="_blank">{{funder.ror}}</a></div>{{/if}}
  {{#if funder.ofr}}<div class="ofr">ofr: {{funder.ofr}}</div>{{/if}}
  {{#if funder.doi}}<div class="doi">doi: <a href="{{funder.doi}}" title="go to doi.org" target="_blank">{{funder.doi}}</a></div>{{/if}}
  {{#if funder.isni}}<div class="isni">isni: {{funder.isni}}</div>{{/if}}
</div>

<div class="labeled_dates">
{{#if funder.updated}}<span class="updated">updated: {{funder.updated}}</span>{{/if}}
</div>

<p>
</section>
{{>footer}}
<!-- {{#if debug_src}}<code><pre>{{debug_src}}</pre></code>{{/if}} -->
<script type="module" src="../js/funder_view.js"></script>
</body>
</html>
